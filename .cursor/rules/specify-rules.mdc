# Plaza-Works Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-16

## Active Technologies
- TypeScript (ESM), Node 18+ + Express, React 18, Vite, Drizzle ORM, TanStack Query, Radix UI (shadcn), Tailwind CSS, Wouter (002-schedule-drag-job-activity)
- PostgreSQL via Drizzle; schema in `shared/models/`, re-exported in `shared/schema.ts` (002-schedule-drag-job-activity)
- TypeScript 5.x, Node.js 20+ (003-remove-multi-org)
- PostgreSQL with Drizzle ORM (003-remove-multi-org)
- TypeScript 5.x, Node.js 20+ + React 18, Express.js, Drizzle ORM, PostgreSQL, TanStack Query, Wouter (004-staff-permissions)
- TypeScript 5.x, Node.js 20+ + Express.js, Drizzle ORM, React 18, TanStack Query, Tailwind CSS (005-role-permissions)
- PostgreSQL via Drizzle ORM (005-role-permissions)
- TypeScript 5.x + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email); existing upload (S3/GCS) if used elsewhere (007-invite-account-setup)
- PostgreSQL (Drizzle); `users` already has firstName, lastName, profileImageUrl; `user_invites` has roleId; add permissions storage on invite (007-invite-account-setup)
- TypeScript 5.x + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email); existing upload (R2/Replit) for profile image (008-teams-invite-admin-config)
- PostgreSQL (Drizzle); `user_invites` extended with firstName, lastName, profileImageUrl; `users` already has these fields (008-teams-invite-admin-config)

- TypeScript 5.6 + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email) (001-admin-invite-users)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.6: Follow standard conventions

## Recent Changes
- 008-teams-invite-admin-config: Added TypeScript 5.x + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email); existing upload (R2/Replit) for profile image
- 007-invite-account-setup: Added TypeScript 5.x + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email); existing upload (S3/GCS) if used elsewhere
- 005-role-permissions: Added TypeScript 5.x, Node.js 20+ + Express.js, Drizzle ORM, React 18, TanStack Query, Tailwind CSS


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
