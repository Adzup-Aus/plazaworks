# Plaza-Works Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-16

## Active Technologies
- TypeScript (ESM), Node 18+ + Express, React 18, Vite, Drizzle ORM, TanStack Query, Radix UI (shadcn), Tailwind CSS, Wouter (002-schedule-drag-job-activity)
- PostgreSQL via Drizzle; schema in `shared/models/`, re-exported in `shared/schema.ts` (002-schedule-drag-job-activity)
- TypeScript 5.x, Node.js 20+ (003-remove-multi-org)
- PostgreSQL with Drizzle ORM (003-remove-multi-org)
- TypeScript 5.x, Node.js 20+ + React 18, Express.js, Drizzle ORM, PostgreSQL, TanStack Query, Wouter (004-staff-permissions)
- TypeScript 5.x, Node.js 20+ + Express.js, Drizzle ORM, React 18, TanStack Query, Tailwind CSS (005-role-permissions)
- PostgreSQL via Drizzle ORM (005-role-permissions)
- TypeScript 5.x + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email); existing upload (S3/GCS) if used elsewhere (007-invite-account-setup)
- PostgreSQL (Drizzle); `users` already has firstName, lastName, profileImageUrl; `user_invites` has roleId; add permissions storage on invite (007-invite-account-setup)
- TypeScript 5.x (aligned with project) (008-integrations-center)
- PostgreSQL via Drizzle (existing) (008-integrations-center)
- swagger-jsdoc + swagger-ui-express for auto-generated API documentation (008-integrations-center)
- UUID v4 for API tokens, bcrypt for token hashing (008-integrations-center)
- Dual auth: Bearer token + session cookie support (008-integrations-center)
- TypeScript 5.x (existing) + Express (existing), Drizzle ORM (existing), QuickBooks Online API (OAuth 2.0 + REST) (009-quickbooks-integration)
- PostgreSQL via Drizzle; new tables for QuickBooks connection and sync mapping (invoice ↔ QuickBooks ID) (009-quickbooks-integration)
- Node.js / TypeScript (existing backend and client stack) + Express, TanStack Query, React 18, QuickBooks OAuth client (`intuit-oauth`), Drizzle ORM (011-quickbooks-integration-expiry)
- PostgreSQL via Drizzle, using existing `quickbooks_connections` and related tables re-exported from `@shared/schema` (011-quickbooks-integration-expiry)

- TypeScript 5.6 + Express, React 18, Vite, Drizzle ORM, TanStack Query, Zod, bcrypt, Resend (email) (001-admin-invite-users)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.6: Follow standard conventions

## Recent Changes
- 011-quickbooks-integration-expiry: Added Node.js / TypeScript (existing backend and client stack) + Express, TanStack Query, React 18, QuickBooks OAuth client (`intuit-oauth`), Drizzle ORM
- 009-quickbooks-integration: Added TypeScript 5.x (existing) + Express (existing), Drizzle ORM (existing), QuickBooks Online API (OAuth 2.0 + REST)
- 008-integrations-center: Added TypeScript 5.x (aligned with project)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
