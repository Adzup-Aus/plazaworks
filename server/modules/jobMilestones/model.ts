/** Job milestones module – schema re-exports from shared for use by this module. */
export {
  jobMilestones,
  insertJobMilestoneSchema,
  type JobMilestone,
  type InsertJobMilestone,
} from "@shared/schema";
