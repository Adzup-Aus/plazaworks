# Nginx server block for Plaza Works
# Domain: app.plazaworks.com.au (DNS on Cloudflare → server IP)
# Enable on server: sudo bash deploy/enable-nginx-site.sh
# HTTPS: certbot --nginx -d app.plazaworks.com.au

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name app.plazaworks.com.au;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
